apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: gcp-database-secret-provider
  namespace: payments-processing
spec:
  provider: gcp
  parameters:
    secrets: |
      - resourceName: "projects/enterprise-core/secrets/db_password/versions/latest"
        path: "db-password.txt"
