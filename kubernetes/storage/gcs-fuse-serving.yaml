apiVersion: apps/v1
kind: Deployment
metadata:
  name: secured-gemma-inference
  namespace: ai-inference
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gemma-serving
  template:
    metadata:
      annotations:
        gke-gcsview.gke.io/mount: "true"
        gke-gcsview.gke.io/bucket: "secured-enterprise-models"
      labels:
        app: gemma-serving
    spec:
      serviceAccountName: ksa-gemma-serving
      containers:
      - name: inference-engine
        image: us-central1-docker.pkg.dev/enterprise-project/vetted-ai/vllm-serving:v1.2
        volumeMounts:
        - name: gcs-mount-volume
          mountPath: /models
          readOnly: true
      volumes:
      - name: gcs-mount-volume
        csi:
          driver: gcsfuse.csi.storage.gke.io
          readOnly: true

# created by @jsaccomani
